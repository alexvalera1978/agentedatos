node_modules/
.env
.env.local
dist/
*.log
npm-debug.log*

# Archivos subidos por los clientes (Excel/CSV) y resultados de pruebas
server/data/uploads/
scripts/results*.json

# Registro de conversaciones (datos del negocio / posible info de clientes)
server/data/chats/

# Config LOCAL de cada cliente (la edita la app; NO debe ir por git para no
# pisar la config de cada entorno ni provocar conflictos al desplegar).
# La GUÍA del agente sí se versiona, en server/data/guides/<id>.txt.
server/data/tenants/*.json
secrets.json

# Sistema
.DS_Store
Thumbs.db

# Documentos del cliente (pueden contener credenciales) — nunca al repo
*.pdf
